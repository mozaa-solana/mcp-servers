[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    unit: pure-logic / mocked tests (no network, no real X API calls)
    integration: hits real X API (requires real OAuth1 tokens; off by default)
